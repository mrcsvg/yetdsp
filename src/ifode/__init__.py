"""iFode -- remuneracao por peca em plataformas de entrega e internacoes no SUS.

Camadas: `extract` (rede), `transform` (limpeza e agregacao), `analyze`
(diagnostico e estimacao). A definicao de caso mora em `ifode.cid`.
"""

__version__ = "0.1.0"
